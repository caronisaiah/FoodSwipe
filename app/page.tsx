import PreferenceOnboarding from "@/components/PreferenceOnboarding";

// Landing + onboarding. Standalone (no bottom nav) for a clean first impression.
export default function Home() {
  return <PreferenceOnboarding />;
}
